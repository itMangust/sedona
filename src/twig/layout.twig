{% set pixelperfect = IS_DEV ? 'class="pixelperfect" style="background-image: url(img/pixelperfect/' ~ page ~ '.jpg)"' %}

<!DOCTYPE html>
<html lang="ru" {{ pixelperfect }}>
<head>
	<meta charset="UTF-8">
	<title>Sedona</title>

	<link rel="stylesheet" href="css/style.css">
	{% if IS_DEV %}
		<link rel="stylesheet" href="css/dev.css">
	{% endif %}
</head>

<body>

	{% block main %}{% endblock %}

	<script src="js/script.js"></script>
	{% if IS_DEV %}
		<script src="js/dev.js"></script>
	{% endif %}
</body>
</html>
